# ❓ brain/qa/ — Kho hỏi-đáp (QA) dùng chung

Mỗi câu hỏi escalate cho PO = 1 file `<slug-câu>-<asker>.md`. **Lưu vĩnh viễn** để sau khỏi hỏi lại
(trí nhớ chống hỏi trùng).

**Trước khi tạo câu mới, BẮT BUỘC check theo thứ tự** (skill `ask-po` lo việc này):
1. Docs version **hiện tại** (`docs/<current_version>/`, ưu tiên `po_docs/`).
2. `brain/qa/` — đã có ai hỏi chưa.
3. Docs version **cũ** (`docs/<version cũ>/`).
4. Hết vẫn không có → mới tạo QA + ghi `brain/ASK-LOG.md`.

Mẫu file: `_TEMPLATE.md`.
